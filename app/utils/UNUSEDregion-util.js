// "use strict";
//
// const cloudinary = require("cloudinary"); // UTIL NOT USED ?????????????????????
//
// const RegionUtil = {
//   returnAllRegionsArray: async function(regions) {
//     let arrayRegions = [];
//     regions.forEach(getRegion); // pass in the users Islands into a forEach and call a function once for each island in the array
//     async function getRegion(region) {
//       // each island that contains an image, pass the ID of the image to the deleteImage function to delete on cloudinary
//       arrayRegions.push(region.region);
//     }
//     return arrayRegions;
//   }
// };
//
// module.exports = RegionUtil;
